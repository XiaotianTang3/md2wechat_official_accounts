export const SAMPLE_MARKDOWN = `# 你还在让 AI 只输出 Markdown 吗？

上个月，一篇讨论 **文档格式与阅读成本** 的文章在开发者圈里被反复转发。很多人开始意识到：*当内容变长、结构变复杂时，纯 Markdown 的天花板会越来越明显。*

如果你用 [CommonMark 规范](https://commonmark.org/) 写过长文，大概能体会那种「能写，但读起来费劲」的感觉。

## 01. Markdown 的天花板

Markdown 的设计目标是 **可读、可写、易 diff**。对短笔记、README、Issue 来说，它几乎是完美的输入格式。

但一旦单篇超过一两百行、层级变多，读者就要在缩进、列表和标题符号里「脑补」结构。协作时还要面对不同渲染器的细微差异。

> 随着 AI 处理的任务越来越复杂，超过 100 行的 Markdown 文件越来越难读——不是语法不够，而是 **视觉层次与版式表达** 跟不上信息量。

下面几点在团队里很常见：

1. 表格一宽，GitHub 预览里就要左右拖动。
2. 想强调层级，只能靠标题级别和加粗，**没有版式语言**。
3. 导出到公众号、飞书时，还要再套一层主题或手工调样式。

## 02. HTML 与富文本的表达力

HTML 并不否定 Markdown 的价值：写作阶段仍可以用 Markdown **换效率**，发布阶段再映射成 **带 inline 样式的 HTML**，以适配微信等富文本环境。

| 能力 | Markdown | HTML |
| --- | --- | --- |
| 表格 | 支持基础表格 | 可表达复杂结构与样式 |
| 颜色与强调 | 几乎无原生支持 | 可通过 inline style 完整表达 |
| 图示 | 多依赖外链图片 | 可内联 SVG 等（视平台限制） |

### 写作仍然应该简单

对内容创作者来说，更合理的分工往往是：

- Markdown（或同类轻量语法）负责 **输入效率**；
- 主题与渲染管线负责 **视觉与品牌一致**；
- 复制/导出负责 **平台适配**。

一段最小的「从格式到输出」示意：

\`\`\`ts
const format = "markdown";
const output = renderToWechatHtml(format);
\`\`\`

实际工程里，\`output\` 应是 **已消毒、已内联主题** 的 HTML 字符串，而不是一堆依赖外部 CSS 的 class。

![排版示意占位](https://placehold.co/800x320.png?text=md2wechat+image+test)

---

**小结**：Markdown 仍是极好的 *写作介质*，但 **复杂内容的交付** 越来越需要 HTML/富文本与可靠的主题管线——这篇示例文用来在两款主题下检查标题、引用、表格与代码块是否顺眼；你可以直接改掉正文，从 localStorage 里会继续存你的草稿。
`;
